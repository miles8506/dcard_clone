import { localStorage } from './setLocalStorage';
import {
  nowTimerFn,
  timeStampFn,
  computedTimeGapFn,
  timeAgoFn,
  dayRange,
  currentDayStamp
} from './dayFormat';

export {
  localStorage,
  nowTimerFn,
  timeStampFn,
  computedTimeGapFn,
  timeAgoFn,
  dayRange,
  currentDayStamp
};
