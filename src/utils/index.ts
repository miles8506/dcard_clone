import { localStorage } from './setLocalStorage';
import {
  nowTimerFn,
  timeStampFn,
  computedTimeGapFn,
  timeAgoFn,
  dayRange
} from './dayFormat';

export {
  localStorage,
  nowTimerFn,
  timeStampFn,
  computedTimeGapFn,
  timeAgoFn,
  dayRange
};
